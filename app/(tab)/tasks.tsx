import { Text, View } from "react-native";

export default function Tasks() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>Tasks</Text>
    </View>
  );
}
